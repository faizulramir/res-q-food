import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'res.q.food',
  appName: 'Res-Q Food',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
